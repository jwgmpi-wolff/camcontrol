# The on-camera payload is uploaded verbatim to busybox ash, which fails on
# CRLF with a confusing "not found". Keep these LF in the working tree too.
src/camera_bridge/oncam/* text eol=lf
sd_bundle_yhs3017-1/camcontrol-install/*.sh text eol=lf
